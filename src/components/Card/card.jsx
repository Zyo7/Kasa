import React from "react";

const Card = () => (
    <div>
        <div>
            <img src="#" alt="Carte temporaire" />
            <p>Titre de la location</p>
        </div>
    </div>
);

export default Card;